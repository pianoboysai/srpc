srpc is a light and fast Python RPC package

*it can be used directly, needn't setup , no third-party dependence

*which support dynamic thread creation, so it can process lots of rpc request, about 10000 RPC calls per second on a regular PC

*the serialization and deserialization is using 'cPickle'£¬so it support almost any data or object transport
