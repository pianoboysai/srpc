srpc is a light and fast Python RPC package

*it can be used directly, needn't setup , no third-party dependence

*it supports thread pool, so it can process lots of rpc request, about 10000 RPC calls per second in a common PC

*the serialization and deserialization is using 'cPickle', so it supports almost any kind of data
